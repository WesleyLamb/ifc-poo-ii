#ifndef WinCheckbox_hpp
#define WinCheckbox_hpp

#include "Checkbox.hpp"

class WinCheckbox: public Checkbox {
    public:
        WinCheckbox();
        ~WinCheckbox();
        void draw();
};

#endif